 
{
  "name": "Angular-MovieSim",
  "version": "1.0.0",
  "description": "rick-liyue-huang Angular-MovieSim",
  "scripts": {
    "e": "echo hello",
    "postinstall": "bower install",
    "prestart": "npm install",
    "start": "./node_modules/.bin/hs -a localhost -p 8888 -o",
    "pretest": "npm install",
    "test": "./node_modules/.bin/browser-sync start --server app --files 'app/index.html' --no-notify"
  },
  "keywords": [
    "rick-liyue-huang",
    "richcode",
    "Angular"
  ],
  "author": "rick-liyue-huang",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "http-server": "^0.9.0",
    "browser-sync": "^2.11.1"
  }
}
















